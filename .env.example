# VoidStream / Nitiflix Environment Variables
# NOTA: el puerto del backend ya NO se define aquí.
# Fuente única: app.config.ts (propiedad APP_CONFIG.port)
NODE_ENV=development
DATABASE_URL="file:./dev.db"
