# SonarQube often hates empty catch blocks without comments inside them! Wait, we added `/* ignore */`.
# Let's ensure the `/* ignore */` is actually well parsed.
# Is there anything else? We changed API_BASE_URL.
